/**
 * Access denied
 * @returns Forbidden page display
 */

const Forbidden = () => (
    <div className="wrap">
        <h2>Forbidden</h2>
        <p>You don't have access to this page.</p>
    </div>
);

export default Forbidden;